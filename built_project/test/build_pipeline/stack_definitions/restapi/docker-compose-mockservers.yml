version: '3.4'
services:
    test_restapi:
      depends_on:
        - test_bbddtest
        - test_mockserver
      networks:
        test_integ_network:
          aliases:
            - test_restapi_external
        test_bakcend:
          aliases:
            - ${DOCKER_RESTAPI_IMAGE_NAME}
      secrets:                    # secrets block only for 'web' service
        - test_bbdd_PASSWORD_SECRET
