       stage('Deploy docker') {
            steps
            {
              withCredentials([usernamePassword(credentialsId: '#jenkins.credentials.id#', usernameVariable: 'CICD_USER', passwordVariable: 'CICD_PASS')])
              {
                //milestone(6)
                  echo 'Desplegamos en PRE'
                  dir('./build_pipeline') {
                      sh './04_deploy.sh -f jenkins -f registrymng_pre -e test'
                  }
               }
            }
       }
