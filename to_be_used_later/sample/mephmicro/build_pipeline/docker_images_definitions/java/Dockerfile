ARG BASE_IMAGE
FROM $BASE_IMAGE
ARG ORIGIN_JAR
ARG DESTINATION_JAR
ARG FINAL_JAR
ARG LABEL

LABEL artifact="$LABEL"

MAINTAINER mephistos

ENV FINAL_JAR $FINAL_JAR
ENV ORIGIN_JAR $ORIGIN_JAR
ENV DESTINATION_JAR $DESTINATION_JAR
COPY $ORIGIN_JAR /data/$DESTINATION_JAR